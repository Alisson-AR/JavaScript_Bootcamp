//Multliplicação 

function multiply(a, b) {
    return a * b;
}

console.log(multiply(2, 4));

//Tamanho da String

function contaCaractere(str) {
    let contaCaractere = 'Alisson';
    return str.length
} 

console.log (contaCaractere("Alisson"));

//Retornar em caixa alta

function gritarNome(nome) {
    let gritarNome = 'Alissooon';
    return nome.toUpperCase();
}

console.log (gritarNome("Alissooon"));


//Retornar em caixa baixa

function caixaBaixa(nome) {
    let caixaBaixa = 'Alissooon';
    return nome.toLowerCase();
}

console.log (caixaBaixa("Alissooon"));